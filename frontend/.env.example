# Copie este arquivo para frontend/.env.local antes de rodar `docker compose up`
# (o docker-compose carrega via env_file e falha se o arquivo nao existir).

# Base URL da API consumida pelo browser
NEXT_PUBLIC_API_URL=http://localhost:4000
